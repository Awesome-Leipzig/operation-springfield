# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: sector-7g-safety-ledger
metadata:
  template: sector-7g-safety-ledger@0.1.0
services:
  web:
    project: .
    language: java
    host: containerapp
    docker:
      path: ./Dockerfile
