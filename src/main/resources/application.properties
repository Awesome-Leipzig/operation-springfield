# Sector 7G Safety Ledger — configuration circa 1997
spring.application.name=sector-7g-safety-ledger
server.port=8080

# H2 in-memory database (the "production" database)
spring.datasource.url=jdbc:h2:mem:snpp;DB_CLOSE_DELAY=-1
# MELTDOWN ALERT: hardcoded credentials in source control (fake, planted for Phase 3)
spring.datasource.username=mr.burns
spring.datasource.password=excellent-hounds-release-1997
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.h2.console.enabled=true

# "Secrets" (also fake, also Phase 3 fodder)
plant.api.key=SNPP-SECRET-KEY-donut-donut-donut-7G
plant.audit.backdoor=i-do-everything-around-here
